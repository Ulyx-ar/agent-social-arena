import { version } from './version.js';

const getEnvironment = () => typeof window !== "undefined" ? "web" : "server";
const SDK_USER_AGENT = `helius-node-sdk/${version} (${getEnvironment()})`;
const getSDKHeaders = () => ({
    "User-Agent": SDK_USER_AGENT,
});

export { SDK_USER_AGENT, getSDKHeaders };
//# sourceMappingURL=http.js.map
